#include <stdio.h>
#include "lib.h"
int main(){
    int a,b;
        oi();
        printf("%i\n",soma(3,3));
        if (ehPrimo(ehPrimo(23)==1))
        {
            printf("eh primo\n");
        }
        else
        {
            printf("nao eh primo\n");
        }
        printf("menor entre 5 e 3 eh %i\n",menor(5,3));
    return 0;
}